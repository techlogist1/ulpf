{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The bundled splash page. The served UI is a remote page and gets no IPC; the shell does everything from Rust.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [{ "name": "binaries/ulpf", "sidecar": true, "args": true }]
    }
  ]
}
